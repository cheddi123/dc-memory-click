import React from "react";

const Title =(props)=>(

<div className="card-header bg-primary text-white text-center  container jumbotron ">
   <h3 > Click on an image but do not click the SAME image twice, otherwise you lose</h3>
</div>


 )

 export default Title